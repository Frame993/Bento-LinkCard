---
const { tags, text } = Astro.props;
---

<div
  class="flex flex-col gap-6 bg-[#191D20] rounded-xl p-8 justify-center w-full"
>
  <h2 class="text-balance md:w-[60%]">
    {text}
  </h2>
  <h2>
    <span>{tags.map((tag: string) => `/ ${tag} `)} </span>
  </h2>
</div>
